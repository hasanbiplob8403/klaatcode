// compareVersions(a, b): compare two "x.y.z" version strings numerically.
// Returns -1 if a < b, 0 if equal, 1 if a > b.
// Missing parts count as 0 ("1.2" equals "1.2.0").
// TODO: not implemented yet.
export function compareVersions(_a: string, _b: string): -1 | 0 | 1 {
  return 0;
}
