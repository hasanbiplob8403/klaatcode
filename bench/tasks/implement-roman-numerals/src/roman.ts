// toRoman(n): integer 1..3999 → Roman numeral string, using standard
// subtractive notation (4 → "IV", 9 → "IX", 40 → "XL", 900 → "CM", …).
// TODO: not implemented yet.
export function toRoman(_n: number): string {
  return "";
}
