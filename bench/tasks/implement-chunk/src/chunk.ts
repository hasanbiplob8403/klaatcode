// chunk(arr, size): split arr into consecutive chunks of `size` elements
// (the last chunk may be smaller). size must be a positive integer —
// throw a RangeError otherwise. Does not modify the input.
// TODO: not implemented yet.
export function chunk<T>(_arr: T[], _size: number): T[][] {
  return [];
}
