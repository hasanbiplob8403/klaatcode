// parseCsv(text): parse a simple CSV string into an array of row objects,
// using the first line as headers. Values are trimmed. Empty input → [].
// TODO: not implemented yet.
export function parseCsv(_text: string): Record<string, string>[] {
  return [];
}
