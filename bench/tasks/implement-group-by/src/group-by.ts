// groupBy(items, keyFn): group items into a Record keyed by keyFn(item),
// preserving item order within each group.
// TODO: not implemented yet.
export function groupBy<T>(_items: T[], _keyFn: (item: T) => string): Record<string, T[]> {
  return {};
}
