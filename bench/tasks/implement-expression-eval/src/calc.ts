// evaluate(expr): evaluate an arithmetic expression string containing
// non-negative numbers (integers or decimals), the operators + - * /,
// parentheses, and spaces. Normal precedence (* and / before + and -),
// left-associative. Throws an Error on malformed input.
// Do NOT use eval() or new Function().
// TODO: not implemented yet.
export function evaluate(_expr: string): number {
  return 0;
}
