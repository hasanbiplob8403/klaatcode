// mergeIntervals(intervals): merge overlapping OR touching [start, end]
// intervals and return them sorted by start. Input may be unsorted and must
// NOT be modified. Touching means [1,2] and [2,3] merge into [1,3].
// TODO: not implemented yet.
export function mergeIntervals(_intervals: [number, number][]): [number, number][] {
  return [];
}
