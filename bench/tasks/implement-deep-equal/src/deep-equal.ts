// deepEqual(a, b): structural equality for JSON-like values (primitives,
// arrays, plain objects). NaN equals NaN. null is only equal to null.
// An array is never equal to a plain object.
// TODO: not implemented yet.
export function deepEqual(_a: unknown, _b: unknown): boolean {
  return false;
}
